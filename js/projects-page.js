import { projects } from './data.js';import {nav,footer,projectCard} from './components.js';import{initShell,renderInto}from'./site.js';renderInto('#nav',nav('projects'));renderInto('#footer',footer());const grid=document.querySelector('#project-grid');grid.innerHTML=projects.map(projectCard).join('');const filters=['All',...new Set(projects.map(p=>p.kicker.split('/')[0].trim()))];document.querySelector('#filters').innerHTML=filters.map((f,i)=>`<button class="filter-btn ${i===0?'active':''}" data-filter="${f}">${f}</button>`).join('');document.querySelectorAll('.filter-btn').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filter-btn').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const val=btn.dataset.filter.toLowerCase();document.querySelectorAll('.project-card').forEach((card,i)=>{const show=val==='all'||projects[i].kicker.toLowerCase().startsWith(val);card.style.display=show?'':'none';});}));initShell();
