import {projects} from './data.js';import{nav,footer}from'./components.js';import{initShell,renderInto}from'./site.js';renderInto('#nav',nav('projects'));renderInto('#footer',footer());const params=new URLSearchParams(location.search);const project=projects.find(p=>p.slug===params.get('project'))||projects[0];document.title=`${project.name} — 0xAeterNova`;document.querySelector('#project-root').innerHTML=`<section class="project-detail-hero"><div class="container project-detail-head"><div class="reveal"><p class="eyebrow">${project.kicker} · ${project.year}</p><h1>${project.name}</h1><p>${project.summary}</p><div class="hero-actions"><a class="btn btn-primary" href="${project.repository}" target="_blank" rel="noreferrer">Open repository ↗</a><a class="btn" href="projects.html">← All projects</a></div><div class="project-facts"><div class="fact"><span>Status</span><b>${project.status}</b></div><div class="fact"><span>Year</span><b>${project.year}</b></div><div class="fact"><span>Type</span><b>${project.kicker.split('/')[0]}</b></div></div></div><div class="project-cover reveal"><img src="${project.image}" alt="${project.name}" width="1200" height="720"></div></div></section><section class="section"><div class="container"><div class="prose reveal"><p class="eyebrow">01 / Overview</p><h2>What it is</h2><p>${project.description}</p></div><div class="prose reveal"><p class="eyebrow">02 / Contribution</p><h2>What I work on</h2><p>${project.contribution}</p></div><div class="prose reveal"><p class="eyebrow">03 / Stack</p><h2>Technologies</h2><div class="skill-cloud">${project.stack.map(x=>`<span class="skill">${x}</span>`).join('')}</div></div></div></section>`;initShell();
